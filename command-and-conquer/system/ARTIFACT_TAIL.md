# WRITE YOUR ARTIFACTS NOW

You have finished your code changes and committed them. It is time to write the 5 artifact files.

Write them to the **EXACT absolute path in the OUTPUT LOCATION section** of this prompt. Do not use a relative path.

The 5 files are: `job_result.json`, `changed_files.json`, `review_checklist.json`, `job_summary.md`, `code_snippets.md`

See **OUTPUT FORMAT** above for the exact schema of each file.

Then write `knowledge_updates/carry_forward.md` inside the same folder.

After writing all files, you are done.
